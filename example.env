# Example environment file for docker-compose
# Copy this file to .env and adjust values before running `docker compose up -d`
# These variables are referenced in docker-compose.yml labels and service configurations.

# Base domain used to construct service DOMAINs:
# API will be available at: https://elo.${DOMAIN}/api
# Client will be available at: https://elo.${DOMAIN}
DOMAIN=example.com

# Optional second domain (no subdomain) that also serves the client and API:
# Client will be available at: https://${SECOND_DOMAIN}
# API will be available at: https://${SECOND_DOMAIN}/api
SECOND_DOMAIN=second-example.com

# Api Configuration
DB_HOST=postgres

# PostgreSQL configuration
POSTGRES_USER=elo
POSTGRES_PASSWORD=ChangeMe_StrongPassword123!
POSTGRES_DB=elo

# Email used by Traefik/Let's Encrypt for certificate registration & renewal notices
LETSENCRYPT_EMAIL=admin@example.com

# (Optional) Add any additional overrides below.
# TRAEFIK_LOG_LEVEL=INFO
